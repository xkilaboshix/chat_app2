<div>
  <ul>
    <% @rooms.each do |room| %>
      <li><%= link_to room.name, room_path(room), data: {"turbolinks" => false} %></li>
    <% end %>
  </ul>
</div>
<div class = "new-book">
  <h2>Rooms</h2>
  <%= form_for(@room) do |f| %>

    <h4>Create room</h4>
    <%= f.text_field :name, class:"col-xs-12" %>



    <%= f.submit 'Create room', class:"col-xs-12" %>

<% end %>
<%= link_to "ログアウト", destroy_user_session_path, method: :delete %>
</div>
